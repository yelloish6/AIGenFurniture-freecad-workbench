# SPDX-License-Identifier: LGPL-2.1-or-later
# SPDX-FileNotice: Part of the AIGenFurniture addon.

[project]
version = '0.1.4' # No need to update
name = 'AIGenFurniture' # these two properties.

requires-python = '>=3.11'

dependencies = [
    'numpy>=1.24.0',
    'numpy-stl>=3.0.0',
    'tripy',
]